import { Bot } from "../../structures/Bot";
import { Module } from "../../structures/Module";

// This is the base module, this consists of all 
module.exports = new class extends Module{

    async init(client: Bot): Promise<void> {
        
    }
}