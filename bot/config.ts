export default {
    prefix: '-sai', // optional, not really required but if you want to run commands without slash then put something here
    botToken: '', // required, https://discordjs.guide/preparations/setting-up-a-bot-application.html#your-token
    clientId: '', // optional, leave empty if slash commands are not needed, which is really needed imo
    devServerID: '', // optional, used for testing slash commands
}